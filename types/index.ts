// TypeScript interfaces and types
export interface Project {
  title: string;
  description: string;
  image: string;
  link: string;
  code: string;
  technologies: string[];
}

export interface FormData {
  name: string;
  email: string;
  message: string;
}
